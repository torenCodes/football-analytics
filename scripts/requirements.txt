# Exact pins, not floors -- a scheduled CI run resolves "latest satisfying
# the constraint" fresh every time with no lockfile, so an unbounded >=
# means a brand-new upstream release can silently change behavior on a run
# nobody's watching. Pinned to the exact versions verified working locally
# (2026-09-15); bump deliberately, not by letting CI float to whatever's
# newest that week.
nflreadpy==0.1.5
pandas==3.0.5
pyarrow==25.0.1
requests==2.34.2
polars==1.43.2  # imported directly by every script -- previously relied on
# nflreadpy's own dependency pulling it in transitively, never declared here
