services:
  # Gridiron Edge -- NFL fantasy + individual-game betting research. One
  # static site for the whole thing (home/fantasy/betting as pages, not
  # separate services) since everything's same-origin.
  - type: static
    name: football-analytics
    staticPublishPath: .
    buildCommand: ""
